# Senra Mobile Redesign — Tracked Plan

> **Home:** this doc lives in the **iOS repo** (`pet-safety-ios/`) as the reference platform, but it is **cross-cutting** and governs both `pet-safety-ios` and `pet-safety-android`. Android-specific refs are pathed from `pet-safety-android/`.
> **Status:** Unit 0 complete (investigation + this doc). No feature code written, no branch created.

---

## 1. Purpose & current status

Restructure the Senra mobile apps so that surfaces which are **already anonymous-capable server-side** (scan → public profile, found-stray report, community / lost-&-found reads, pet-friendly-places discovery) become reachable **without logging in**, behind a new logged-out **landing** screen. Fix the guest tag-ordering dead-end (mobile drops the `userId` the checkout needs) and correct inaccurate OTP copy.

**Current status:** Phase 0 (read-only investigation) is **done** — findings in §5, gaps in §6. Nothing is built. Next unit is Phase 1 (shell + splash + landing), pending review of this doc.

**Working model (two-seat):** CC investigates/builds/surfaces; chat reviews diffs byte-level; **Viktor owns all git**. One reviewed unit per commit. Unit N+1 does not start until unit N is reviewed, tested, and committed.

---

## 2. Scope

### In scope
- Guest/public pre-login access to surfaces that are **already anonymous-capable server-side**: scan → public profile, found-stray report, community / lost-&-found reads, pet-friendly-places discovery.
- New **landing** screen (default logged-out state) + refreshed splash.
- **Guest tag-ordering wiring:** capture `userId`/`email` from `createOrder` and thread into `CreateTagCheckoutRequest` on the **guest path only** (authed order path untouched).
- **OTP copy fix** ("check your email for a code" is inaccurate — no code is sent until the user requests OTP at login).

### Out of scope / deferred
- **ANYTHING that reads, writes, or maps into the NAV / Számla invoicing flow.** Separate, parallel work. Invoicing gaps are **documented here (see §6), never actioned here.**
- **Separate billing/shipping ADDRESS CAPTURE (Phase 4)** — DEFERRED until the invoicing work lands, because its only value is on the invoice, which is across the boundary.
- **B2B invoicing identity** (future `orders.billing_info` column) — out.
- Any net-new feature beyond access-restructuring.

### Locked decisions
- **Shell approach = Option A:** add a logged-out-default branch so a new `LandingView` becomes the default unauthenticated state, leaving `MainTabView` / `MainTabScaffold` untouched.
  - iOS: near-drop-in at `ContentView.swift:15–33`, gated solely on `authViewModel.isAuthenticated`.
  - Android: near-drop-in at `PetSafetyApp.kt:275–318` — a single `screenKey` `when`-block. Add a `"landing"` case; no nav-graph rewrite.
- **Public surfaces gate at the ACTION level, not the screen level:** view public; actions like create-alert / submit-place / activate-tag prompt login at the point of action, reusing the existing deep-link-sheet pattern (iOS `ContentView.swift:53/:84`; Android the existing activation/login-prompt route).
- **Small, independently testable chunks; one surface per chunk in Phase 2.**
- **Logged-out affordances are strictly limited (Q1 — decided 2026-07-09):** an unregistered / logged-out finder gets exactly **two** actions and no others — (1) **Report a sighting** (the existing sighting flow) and (2) **Scan a tag** (the existing scan flow → the found pet's public profile page). No edit, no mark-found, no other owner/account actions are shown to logged-out users. On the missing-pet **alert detail** this means it renders as a public read exposing **only** those two actions; every `currentUser`-gated affordance is hidden when logged out.
- **Landing structure = Shape A, "three-zone community home base" (Q5 — decided 2026-07-10):** the default logged-out landing is a three-zone layout designed so future community features **append to an existing structure** rather than trigger a landing redesign:
  - **Zone 1 — Act now** (compact action row, top): **Scan a tag** + **I-found-a-stray** — prominent, fast, visually tight (the urgent-finder entry). Both trivial/anonymous (§4 2.1 / 2.2).
  - **Zone 2 — Order a tag** (distinct product CTA, at the seam): first-class, styled **distinctly** from the acute actions (product/conversion intent, not finder intent). Entry to Phase 3.
  - **Zone 3 — Community** (the growable body): a titled section that **renders a list of community entries** — seeded with two today (Lost & Found board, Pet-friendly places). Built as a **data-driven rendered collection** (array of entry descriptors → cards), **not** two hardcoded tiles. Adding a future community feature = appending an entry, **no landing-layout change**. Board/places reads are anonymous (§4 2.3 / 2.4).
  - **Persistent — Sign in / Register:** quiet, secondary, always reachable.
  - Two guardrails are the point of Shape A: **(G-a)** **no empty "coming soon" placeholders** in the community section — "room to grow" means the list is *extensible* (structural, invisible until used), presenting as a complete two-entry section, not visible empty slots; **(G-b)** community card/list styling **must reuse existing components** from the §5.3 inventory — Shape A leans harder on shared components than a flat landing, so the Phase-1 spec must **name** which existing components the community cards reuse, and if none fit cleanly that is a finding **CC surfaces before building**, not a new component minted mid-chunk.
- **Android carries a POINTER, not a copy (decided 2026-07-17 — extends Q6, does not amend it).** An Android-seat session opened in `pet-safety-android` cannot see the protocol, the plan, the spec, or the board — every document governing its work lives in `pet-safety-ios`. **The fix is a pointer, never a mirror.** Q6 ruled out a copy for the plan; the same reasoning binds here — a copy drifts from its original within a week, and that drift is the failure the protocol exists to kill. So `pet-safety-android/SENRA.md` (`f0c5aa3`, 2026-07-17) **states no facts**: it points at `pet-safety-ios/docs/{PROTOCOL,SENRA-MOBILE-REDESIGN,phase-1-spec}.md` + `scripts/senra-status.sh`, and says to run the board from the iOS checkout. It is the **HANDOVER pattern applied cross-repo** — a file with nothing in it to be wrong about cannot go stale.
  - **The board needed no change and got none.** It already reads both repos — `senra-status.sh:17-19` resolves `IOS`/`AND`/`DL` from the environment, so it runs correctly *from the iOS repo against Android today*. There is no Android board and none was minted. The gap was never "the board can't see Android"; it was "an Android seat can't see the docs."
  - **What this forbids:** copying, mirroring, or symlinking any governing doc into `pet-safety-android`. If the pointer ever needs to state a fact to be useful, that is the signal the fact is in the wrong document — move the *reader*, not the *text*.

---

## 3. Working discipline

- Read-only tracing precedes every build unit; findings land here before code.
- One surface per Phase-2 chunk; each chunk ships with tests (harness confirmed strong on both platforms — §5.5).
- **iOS and Android ship the same UX iteration** unless explicitly scoped down (feature exists on both).
- Invoicing findings are **documented in §6 and never actioned** in this workstream.
- No guessing: inconclusive traces are marked "inconclusive" with what would resolve them.

---

## 4. Plan — phases & chunk skeleton

> Phase 0 is complete, so most chunk internals below are now **specifiable** (see §5). Items still needing a decision are flagged and tracked in §7.

### Phase 0 — Investigation ✅ (this unit)
Auth-boundary trios (both platforms), public-surface dependency audit (both), design-token inventory (both), OTP copy audit, test-harness confirmation. → §5.

### Phase 1 — Shell + splash + landing
> **Buildable chunk spec:** [`docs/phase-1-spec.md`](docs/phase-1-spec.md) — finalized a/b split + a leading splash chunk + build order, the G-b component-reuse resolution (`CommunityEntryCard`), the cross-phase seam (Zone-3 intent + Phase-2 destination edge), the Community entry-descriptor shape, and per-chunk files/tests/done-when.
- **1.1 iOS shell/landing** — the new `LandingView` renders the **Shape-A three zones** (§2): Act-now row (Scan + found-stray), a distinct Order-a-tag CTA, and the data-driven Community list (Lost & Found + Pet-friendly, rendered from an entry-descriptor array). Reuse `WelcomeView.swift` structure + `PrimaryPillButtonStyle` + `LocalizedLogo` + `AppSpacing`/`AppColors` (§5.3); the chunk spec must name the components the Community cards reuse (G-b). **Split steer** (to finalize when the Phase-1 chunk spec is written):
  - **1.1a — shell/routing + splash:** add the logged-out-default branch to `ContentView.swift:15–33` (Option A) with `LandingView` as the default unauthenticated state and `AuthenticationView` reached from a landing CTA; refresh `SplashScreenView.swift` **in place**. Carries the **existing** logout/session-expiry acceptance checks (below) — structural, testable in isolation.
  - **1.1b — landing content/layout:** the three-zone layout + the data-driven Community collection (the iterative visual design).
  - *Rationale:* keep the structural routing change (testable in isolation) separate from the iterative visual landing design — don't couple a structural diff to a design diff.
  - **Done-when (acceptance — 1.1a, mandatory before build sign-off):** default logged-out launch shows `LandingView`; **logout routes to `LandingView`** (not the old `AuthenticationView`, not a blank screen); **session-expiry routes to `LandingView`**. This is a behavioral change riding along with the gate edit and must be tested **explicitly**, because the iOS **optimistic** path (`checkAuthStatus` sets `isAuthenticated=true` on token presence, then `logout()` on a failed `getCurrentUser`) can bounce a user **mid-session** — that bounce must land on `LandingView`. Cover both the explicit-logout and the mid-session-expiry transitions with XCTest/UI test.
- **1.2 Android shell/landing** — the new Landing composable renders the **Shape-A three zones** (§2): Act-now row (Scan + found-stray), a distinct Order-a-tag CTA, and the data-driven Community list (rendered from an entry-descriptor array). Reuse `BrandButton`, `BrandCard`, `LocalizedLogo`, `DesignTokens`/`Color.kt` (§5.3); the chunk spec must name the components the Community cards reuse (G-b). **Split steer** (to finalize when the Phase-1 chunk spec is written):
  - **1.2a — shell/routing + splash:** add a `"landing"` case to the `PetSafetyApp.kt:275–318` `screenKey` `when`; refresh splash via the system SplashScreen theme (`themes.xml` `Theme.PetSafety.Splash`) and/or a new post-splash composable. Carries the **existing** logout/session-expiry acceptance checks (below).
  - **1.2b — landing content/layout:** **build the new Landing composable** (none exists) with the three zones + the data-driven Community collection.
  - *Rationale:* keep the structural routing change separate from the iterative visual landing design.
  - **Done-when (acceptance — 1.2a, mandatory before build sign-off):** default logged-out launch shows the new Landing; **logout routes to Landing** (not `AuthScreen`, not blank); **session-expiry routes to Landing** — today `TokenAuthenticator`'s one-shot expiry drives the non-dismissible "Session Expired" dialog + logout, so verify that after that path `screenKey` resolves to the new landing branch, **not** the old `"auth"`/`AuthScreen`. Cover both transitions with the Robolectric / `compose.ui.test` harness.
- *Asymmetry:* iOS has `SplashScreenView` **and** `WelcomeView` to refresh in place; Android has **neither** a Compose splash nor a landing composable — both built new from shared tokens.

### Phase 2 — Route public surfaces (one surface per chunk, ordered trivial→heavy)
Each chunk = hoist the surface out of the authed shell + optionalize any soft `currentUser` read + gate authed actions at action level. Mirror on both platforms.
- **2.1 Scan → public profile** — *Trivial.* iOS live views carry no `@EnvironmentObject`; `DeepLinkScannedPetView` already renders at `ContentView` level (works logged-out today). Android: hoist `QrScannerScreen`/`ScannedPetSheet`/`PublicPetProfileScreen` out of `MainTabScaffold`; make `PublicPetProfileScreen` take `currentUser: User?` (nullable) and drop its unused `appStateViewModel` param. **Side task:** delete/quarantine dead iOS `ScannedPetView` (§6-G7).
- **2.2 Found-stray report** — *Trivial.* Both platforms auth-optional by design (anonymous manage token). Wire entry from landing/board.
- **2.3 Lost & Found board** — *Light.* iOS `AlertsTabView`: optionalize the `currentUser` GPS-fallback geocode + rework the `AddressRequiredView` CTA. Android: render `LostAndFoundScreen` directly with a device `userLocation`, or drop the 2 unused VMs + optionalize the fallback in `AlertsTabScreen`. **Kill dormant legacy alerts views first** (§6-G6).
- **2.3b Missing-pet alert detail (public read)** — Per the locked decision (§2), the detail (`AlertDetailView` / Android detail) renders publicly with **only Report-a-sighting + Scan-a-tag** for logged-out users; the `currentUser?.id` ownership branches (Edit, Mark-Found) are hidden when logged out. Split from 2.3 because those ownership branches must be optionalized. **Q2 RESOLVED → fully client-only** — the backend `POST /alerts/:alertId/sightings` is already `optionalAuth` with a nullable `reporter_id` and user-less client DTOs (§5.6); **no backend change**. **Report-sighting entry differs by platform:** iOS **relocates existing UI** (`ReportSightingView`, reached today from the authed `AlertDetailView:274`) into the public detail; Android **builds the entry** from the anonymous-capable repo method (`AlertsRepository.reportSighting`), **lifting the form logic from the dormant `ReportSightingDialog` per G6 — not resurrecting the dead `AlertsScreens.kt`**. Scan-a-tag reuses the existing scanner / deep-link path.
- **2.4 Pet-friendly places discovery** — *Light read + one authed action.* Read is anonymous; derive `market` from `Locale.current.region`/device country. **Gate submit-place behind an action-level login prompt** (`createPetFriendlyPlace` is `requiresAuth:true`). **Parity note:** the two platforms derive `market` slightly differently today (iOS `Locale.current.region`; Android `currentUser.country ?? Locale`), so the **logged-out/anonymous path on both must fall back to the device locale identically** — a logged-out user in HU must get HU places on both apps (do not let one platform diverge to a hardcoded/empty market when `currentUser` is null).

### Phase 3 — Guest-order wiring + copy fix
- **3.1 iOS guest-order wiring** — Capture `userId` (+ `email`) from the `createOrder` response and add `user_id`/`email` to `CreateTagCheckoutRequest` (`Order.swift:194`), threaded on the **guest path only**. Mirrors web `GetYourTag.tsx:444→458`. Authed path untouched. No backend change (route is `optionalAuth`).
- **3.2 Android guest-order wiring** — Same: `OrdersViewModel.createOrder` captures `response.userId`; extend `CreateTagCheckoutRequest` (`Requests.kt:279`) + `OrdersViewModel.createTagCheckout` signature; thread on guest path.
- **3.3 OTP copy fix** — On mobile there is **no inaccurate string today** (§5.4); the fix is to ensure the guest-order success/account-created surface built in 3.1/3.2 uses **accurate** copy (never claims a code was emailed). Wording pending HU canonical (§7-Q3). Web inaccuracy + backend response message documented (§6-G4), out of mobile-build scope.

### Phase 4 — DEFERRED
Separate billing/shipping **address capture**. Blocked until invoicing work lands (its only value is on the invoice, across the boundary). Do not build here.

---

## 5. Current-state findings (Phase 0)

### 5.1 Auth-boundary trio — iOS (reference) vs Android

| Concern | iOS | Android |
|---|---|---|
| **Shell / gate** | `PetSafetyApp` → Splash → `ContentView` → `MainTabView`. Gate: `if authViewModel.isAuthenticated { MainTabView } else if showRegistration {…} else { AuthenticationView }` — `ContentView.swift:15–33`. | `MainActivity:50–68` → `PetSafetyApp` → `MainTabScaffold`. Gate: `screenKey` `when`-block `PetSafetyApp.kt:275–318` (`isAuthenticated→"main"`, `showOrderTagsScreen→"order_tags"`, `showRegisterScreen→"register"`, else `"auth"`=`AuthScreen`), via `AnimatedContent` crossfade. |
| **Logged-out default** | `AuthenticationView` | `AuthScreen` (order-tags/register are toggled sub-branches) |
| **Session store** | `AuthViewModel.isAuthenticated` (`@Published`), backed by Keychain (`KeychainService.isAuthenticated = exists(.authToken)`, `KeychainService.swift:231`). **Optimistic**: set true on token presence, then revalidated async via `getCurrentUser()`; failure → `logout()`. | `AuthTokenStore` (EncryptedSharedPreferences, AES256) is source of truth; `AuthRepository.isAuthenticated = authToken.map { !isNullOrBlank() }` (`AuthRepository.kt:44`); `AuthViewModel` mirrors. **Reactive, not optimistic-revalidate** — a failed `users/me` only logs; demotion happens **only** via a real 401. `AppStateViewModel` holds **no** auth. |
| **Token attachment** | Central `buildRequest(requiresAuth: Bool = true)` (`APIService.swift:225–303`); `Bearer` attached only `if requiresAuth, let token`. **Per-call opt-out on 17 endpoints.** App Check header on all; 401 auto-refresh via `TokenRefreshCoordinator` actor. | **Global `AuthInterceptor`** (`AuthInterceptor.kt:12–33`) attaches `Bearer` to **ALL** requests whenever a token exists — **no per-call `requiresAuth`** (verified: no `@Header`/`requiresAuth` in `ApiService.kt`). 401 → OkHttp `TokenAuthenticator` (refresh + one-shot expiry dialog). Also unconditional `AppCheckInterceptor` (can fail-closed 503) + Remote-Config base-URL rewrite. |
| **Token-less calls** | 17 endpoints pass `requiresAuth:false` (auth bootstrap, config, qr lookup/scan, share-location, community found-pets, pet-friendly reads, nearby alerts, vaccine catalog, guest order create/validate-promo/shipping-prices). | Same endpoints go through the shared client; anonymous = simply no stored token. **No client-side auth gate on the call** — the backend's `optionalAuth` decides. |

**Key divergence for the redesign:** Android attaches the token to every request via one interceptor (no `requiresAuth` seam), so anonymous behavior is purely "no token present." Both platforms' gate is a single-branch decision → **Option A landing is near-drop-in on both.**

**Reachability hazard (both):** most public-endpoint-backed screens live *inside* the authed shell (`MainTabView` / `MainTabScaffold`), so they're gated at the **navigation** level even though their endpoints need no token. Phase 2 = hoist them out.

### 5.2 Public-surface dependency audit → Phase-2 chunk list

Ground truth: **every public surface's own ViewModel is auth-free**, and every read works anonymously at the transport layer. Only soft, null-safe `currentUser` reads exist, all degrading gracefully.

| Chunk | Surface | iOS status | Android status | Weight |
|---|---|---|---|---|
| 2.1 | Scan → public profile | `QRScannerView` (no env obj), `DeepLinkScannedPetView` (already renders at `ContentView` level, logged-out today), `ShareLocationView` — all anonymous | `QrScannerScreen` (appState snackbar param only), `ScannedPetSheet` (stateless), `PublicPetProfileScreen` (reads `currentUser` **null-safe** `:91,183`, + unused `appState` param) | **Trivial** |
| 2.2 | Found-stray report | `FoundPetFormView` / `FoundPetDetailView` — no env obj, auth-optional, manage token | `FoundPetFormScreen` / `FoundPetDetailScreen` — VM injects repo + `FoundPetManageTokenStore`, no auth | **Trivial** |
| 2.3 | Lost & Found board (LIVE) | `AlertsTabView` (`ContentView.swift:221/:272`) reads `authViewModel.currentUser` **only** for GPS-fallback geocode (soft); `LostAndFoundViewModel` fully auth-free. ⚠️ row → `AlertDetailView` has **hard** `currentUser?.id` ownership dep | `LostAndFoundScreen` + `LostAndFoundViewModel` fully public; wrapper `AlertsTabScreen` reads `currentUser` (fallback only) + instantiates **2 unused VMs** | **Light** (board) |
| 2.4 | Pet-friendly places | `PetFriendlyPlacesView` reads `currentUser` for `market` (has `Locale` fallback) + geocode fallback; **submit-place = `createPetFriendlyPlace` `requiresAuth:true`** → action-level login | `PetFriendlyPlacesScreen` + VM auth-free; `market` derived caller-side from `currentUser.country ?? Locale`; submit-place is the authed action | **Light read + 1 authed action** |

**Action-level login prompts belong on:** activate-tag / promo-claim (scanner) and submit-place (pet-friendly). iOS additionally: viewing the missing-pet **detail** (`AlertDetailView`) is authed today — scope decision in §7-Q1.

### 5.3 Design-token / shared-component inventory (reuse, don't reinvent)

**iOS** (`PetSafety/PetSafety/`):
- Colors — `Utilities/AppColors.swift`: `brandOrange` (asset `BrandColor` #FF914D, :26), `brandOrangeDeep` (#E5662C, :37), `brandGradient` (:42–48), plus asset-catalog colors `cream`/`ink`/`softBorder`/`mutedText`/`tealAccent`/`peachBackground`/`goldenAccent`/`cardBackground`; `BackgroundColor` (splash bg).
- Spacing/Radius — `AppSpacing` (:61–68), `AppRadius` (:70–76).
- Type — `Font+App.swift`: `Font.appFont(size:weight:)` (Inter).
- Components — `PrimaryPillButtonStyle` (**new default CTA**, AppColors.swift:169–199), `SecondaryPillButtonStyle`, `.softCard`/`.elevatedCard`, `BrandButtonStyle` (legacy), `Views/Components/CachedAsyncImage.swift`.
- Logo — `Helpers/LocalizedLogo.swift` (`LogoNew_<CC>`, fallback `_EN`).
- **Refresh in place:** `Views/SplashScreenView.swift` (splash) and `Views/Pets/WelcomeView.swift` (the existing landing pattern — mirror it).

**Android** (`app/src/main/java/com/petsafety/app/`):
- Colors — `ui/theme/Color.kt`: `BrandOrange` #FF914D (:21), `BrandOrangeDeep` #E5662C (:26), `Cream`/`Ink`/`SoftBorder`/`TealAccent`/`PeachBackground`/`GoldenAccent` + dark variants; `Theme.kt` `PetSafetyTheme`.
- Type — `ui/theme/Type.kt` (Inter, phone/tablet/adaptive).
- Tokens — `ui/theme/DesignTokens.kt`: `AppSpacing`, `AppRadius`, `brandGradient()`, `Modifier.softCard()/elevatedCard()`; `Shape.kt`; `WindowSizeUtils.kt` (`AdaptiveLayout`).
- Components — `ui/components/`: `BrandButton` (primary gradient pill), `SecondaryButton`, `TealButton`, `BrandCard`, `BrandTextField`, `AdaptiveContainer`.
- Logo — `util/LocalizedLogo.kt` (`R.drawable.logo_new_<cc>`).
- **Build new:** no Compose splash (only system SplashScreen theme `themes.xml` `Theme.PetSafety.Splash`) and **no landing/welcome composable** — both created new from the above.

### 5.4 OTP copy audit (⚠️ refines the seeded gap)

**The inaccurate "check your email for a code" post-order copy does NOT exist on mobile.** It is **web-only**:
- `tagme-now/src/pages/redesign7/GetYourTag.tsx:448` — hardcoded HU `defaultValue: "Fiók létrehozva. Ellenőrizd az e-mail fiókodat a megerősítő kódért."`
- `tagme-now/src/i18n/locales/hu.json:351` — `"account_created": "Fiók létrehozva! Ellenőrizd az e-mailedet a további lépésekhez"`
- (Origin also server-side: the `POST /orders` response `message` at `backend/src/routes/order.routes.ts:211–213` — "Account created. Please check your email for verification code.")

On **iOS + Android**, every "code"/"verification" string found is **accurate login-OTP copy** — `verify_code`, `enter_email_subtitle` ("…receive a login code"), Android `code_sent_to_email` ("Code sent to %1$s"), FAQ `help_faq_a15`. None claims a code is emailed after ordering, because the mobile guest-order flow currently dies at checkout (§6-G5) and never reaches an account-created message.

→ **On mobile the copy fix is latent, coupled to Phase 3** (nothing to edit until the success surface exists). **To-verify (§7-Q4):** whether Android's `createOrder` callback surfaces the backend English `message` (iOS discards it).

### 5.5 Test-harness confirmation

- **iOS** — XCTest. **43** test files in `PetSafety/PetSafetyTests/` + 3 UI tests (incl. `AccessibilityAuditTests`). Directly relevant precedents: `AuthViewModelTests`, `AlertsViewModelTests`, `OrdersViewModelTests`, `PetFriendlyPlacesViewModelTests`, `DeepLinkTests`, `LocalizationParityTests`.
- **Android** — JUnit + **MockK + Turbine + Robolectric 4.14.1 + `compose.ui.test` (JVM)**; **59** unit tests in `app/src/test/` (incl. `AuthViewModelTest`, `AlertsViewModelTest`, `OrdersViewModelTest`, `PublicPetProfileViewModelTest`, `QrScannerViewModelTest`, `AppStateViewModelTest`, screen tests like `MarkAsMissingScreenTest`), + **2** Espresso instrumented tests in `androidTest/`.
- **Verdict:** per-chunk "tested before we proceed" is concrete on both — ViewModel + Compose/SwiftUI-view unit tests. Instrumented/E2E is thin on Android (2), but unit coverage is the lever.

### 5.6 Q2 + Q4 verification (2026-07-10)

**Q2 — report-a-sighting is anonymous-capable end-to-end → verdict (A): no backend change.** Chain verified backend → DB → both clients:
- `POST /alerts/:alertId/sightings` (`backend/src/routes/alert.routes.ts:281`) is **`optionalAuth`** (`:282`), not `authenticateToken` (contrast: `/missing`, `/found`, `PUT /:id`, and owner-only `GET`/`DELETE /:id/sightings` are all `authenticateToken`). Handler: `reporterId = req.user ? req.user.userId : undefined` (`:321`); activity log runs only `if (reporterId)` (`:336`); no ownership check.
- `alertService.reportSighting` takes `reporterId?` optional + null-safes the reporter email; the DB column is **nullable** — `reporter_id UUID REFERENCES users(id) ON DELETE SET NULL` (`backend/migrations/00000000_base_schema.sql:242`). An anonymous sighting inserts cleanly.
- Both client DTOs carry only reporter-contact + location, **no user field** (`iOS Models/Alert.swift:324`; `Android Requests.kt:89`). iOS `reportSighting` (`APIService.swift:947`) keeps the default `requiresAuth:true` but that attaches a token only if one exists, so a logged-out call goes token-less; Android sends via `AuthInterceptor` (token only if present). → the sole blocker is UI reachability, which 2.3b decouples. **Android caveat:** report-sighting UI lives only in the dormant `ReportSightingDialog` today (not on the live `LostAndFoundScreen`), so Android 2.3b builds the entry from the repo method (see §4 2.3b + G6).

**Q4 — Android discards the backend order `message`, matching iOS.** `OrdersViewModel.createOrder`'s callback `message` param is the *error* string (`ex.localizedMessage`), never `response.message`; `OrderMoreTagsScreen.kt:818–832 / 853–865` reads `response` only to proceed to checkout. The post-order success toast is a local, accurate string (`R.string.checkout_tag_order_success`, `:119/:130`), not the backend copy. → no live inaccurate mobile string; G4 stays latent (folded into Phase-3.3).

---

## 6. Gaps register — documented, NOT fixed

> Invoicing items are **out of scope** for this workstream (§2). Recorded here only so they aren't lost; route any action to the separate invoicing work.

- **G1 — Invoice mapper ignores `billing_address`** *(invoicing territory — document only).* The order invoice buyer is assembled from `orders.shipping_address` **exclusively**; `billing_address` is never read (`backend/src/services/invoicing/orderSourceAssembler.ts:61`, `:64–76`). Distinct B2C billing is captured but never invoiced. Confirmed this investigation.
- **G2 — `orders.billing_address` is unstructured** *(invoicing territory).* `createOrderSchema` types it as free-form `z.record(z.string(), z.unknown()).nullish()` (`backend/src/routes/order.routes.ts:79`). Needs a structured shape before it could be safely mapped.
- **G3 — B2B invoice branch awaits a future column** *(out of scope).* The mapper's B2B branch exists (`backend/src/services/invoicing/orderInvoiceMapper.ts:114–125`) but reads a future `orders.billing_info` (companyName/taxNumber), **distinct from `billing_address`**.
- **G4 — Inaccurate "check your email for a code" copy** *(copy fix IN scope for mobile).* Guest order creates a **silent passwordless account** (`users.password_hash = null`, `backend/src/services/orderService.ts:180`); **no OTP is sent at order time** — "Account created silently … OTP will be sent on-demand when the user requests to log in" (`orderService.ts:192–194`). Inaccurate copy lives in **web** (`GetYourTag.tsx:448`, `hu.json:351`) and the **backend response message** (`order.routes.ts:211–213`); **mobile has none today** (§5.4). Mobile fix is Phase 3 (accurate copy on the new success surface).
- **G5 — Guest tag-order dead-ends at checkout on mobile** *(fix IN scope — Phase 3).* Backend `POST /orders/create-checkout` is `optionalAuth` and accepts body `user_id`/`email` for unauthenticated checkout (`order.routes.ts:983–1008`), but **both mobile clients discard the `userId` from `createOrder` and their `CreateTagCheckoutRequest` has no `user_id`/`email` field** (`iOS Order.swift:194–210`; `Android Requests.kt:279–286`) → guest checkout 401s. Web threads it correctly (`GetYourTag.tsx:444→458`). Guest ends up **token-less with a real account**, OTP-in later (never auto-logged-in). `delivery-points` is also `optionalAuth` → **not** a guest wall (earlier assumption corrected).
- **G6 — Dormant alerts/pricing screens** *(wiring hazard for Phase 2.3 — confirmed).*
  - iOS: `AlertsListView`, `MissingAlertsView`, `FoundAlertsView`, `SuccessStoriesView`, **and** `SuccessStoriesTabView` — each referenced **only** in its own `#Preview`; all carry live `authViewModel`/`appState`/`currentUser` couplings. The LIVE board is `AlertsTabView`.
  - Android: `AlertsScreens.kt` (`MissingAlertsScreen`/`FoundAlertsScreen`/`AlertDetailScreen`/`ReportSightingDialog`) and `PricingScreen.kt` — zero live nav references. The LIVE board is `LostAndFoundScreen`.
  - **Must wire to the LIVE unified board; delete/quarantine the dead views so none is mistaken for it.**
- **G7 — Dead iOS `ScannedPetView`** *(new, Phase-2.1 hazard).* `Views/QRScanner/QRScannerView.swift:299` — a parallel unused scan-result variant (fed by `ScanResponse`); only self-reference. Live scan-result path is `DeepLinkScannedPetView` (fed by `TagLookupPet`). Decide delete vs. adopt.
- **G8 — Android `AlertsTabScreen` unused VMs** *(new, minor).* Instantiates `QrScannerViewModel` + `SuccessStoriesViewModel` that are never used; drop during Phase 2.3.
- **G9 — iOS deep-nav gap** *(document; relevant if landing deep-links these on iOS).* `NotificationHandler` posts `navigateToAlert`/`navigateToPet`/`navigateToScan` but **no view consumes them** (Android does). If the landing/public flow ever relies on those routes on iOS, they'd be no-ops until wired.
- **G10 — Home entry-card duplication (deferred dedup)** *(surfaced by the Phase-1 G-b traces; documented, not actioned).* The icon+title+subtitle+chevron entry-card pattern is duplicated **inline** on both platforms — iOS `PetsListView.swift:436-465` (Success Stories) + `:477-522` (Pet-Friendly); Android `PetsListScreen.kt:855-935` (`SuccessStoriesSection`) + `:941-1015` (`PetFriendlyPlacesSection`) — none extracted. Phase 1 builds a shared `CommunityEntryCard` for the **landing only** and deliberately does **not** refactor the authed home to consume it (that touches `MainTabView`/`MainTabScaffold` internals — out of Phase-1 scope, minimal blast radius). Future dedup: have the authed home consume `CommunityEntryCard`. → separate refactor, not this workstream.
- **G11 — ✅ RESOLVED 2026-07-14 → closes in C4. Logged-out pending scan is stranded on Android (a cross-platform PARITY gap, not an open question).** *(Surfaced 2026-07-12 by C0's true gate; pre-existing, not a C0 regression.)*
  - **What it is, two-ended.** Android captures a cold-start tag QR **above** the splash gate — written at `PetSafetyApp.kt:77` (`savedQrCode = pendingQrCode`), held in `rememberSaveable` at `:73`. It is **read at exactly one site**: `PetSafetyApp.kt:312` (`pendingQrCode = savedQrCode`), passed into `MainTabScaffold` inside the `"main"` arm of `when (target)` (`:309`) — reachable **only** when `screenKey == "main"` ⇒ `isAuthenticated` (`:295`). The `"order_tags"`, `"register"` and `"auth"` arms never touch it. **Logged out, the scan evaporates.**
  - **iOS does NOT have this bug, and never did.** Its three deep-link sheets hang on the `Group` at `ContentView.swift:42`, **outside** the `switch route` — route-independent. `showScannedPetProfile` (`:80`) renders `DeepLinkScannedPetView` with **no auth branch** (`:84`). A logged-out iOS scan simply presents the pet. **So this is a platform divergence that has been shipping**, not something Phase 1 creates. Phase 1 only makes it intolerable: after C2, Android's logged-out default is a landing whose whole purpose is public scan, and the one thing a scanned tag does there is nothing.
  - **⚠️ The scope guard that failed.** G12b said *"do not wire the dormant `ScannedPetView`; logged-out delivery is a Q-level Phase-2 decision."* The guard was honored — nobody touched `ScannedPetView`. **Logged-out delivery arrived anyway on iOS**, through the *live* `DeepLinkScannedPetView` on a container-level sheet, one branch over. **A scope guard that names a FILE instead of a BEHAVIOR guards nothing.** Same shape as `handleDeepLink` being two unrelated functions. Recorded as **G-guard** below.
  - **Why it closes in C4, not Phase 2** *(verified 2026-07-14; supersedes an earlier deferral)*: `scanResult` has **one writer** — `QrScannerViewModel.lookupAndRoute(code: String)` (`:49`, writing at `:65`) — which is keyed on a **raw `String`**, not a camera frame. It already has **two callers**, both passing a `String`: the camera path (`QrScannerScreen.kt:190-193`, which reduces the frame to a String first) and **the deep-link path** (`QrScannerScreen.kt:148-151`, driven by the `pendingQrCode: String?` param at `:111`). `QrScannerScreen` is a **standalone composable** (`:109`), and **spec §C.1 already requires C4 to present it modally from the landing's Zone 1 — explicitly _not_ in the tab scaffold.** So C4 does not need to mint a public surface; it **drags one out of the scaffold as a side effect of a Zone-1 requirement that was already locked.**
  - **C2's obligation (in-bounds, and it is only this):** do **not** null, relocate, or wire `savedQrCode`. Preserve the hold. **Guard it mechanically, not with a test** — `grep -c 'savedQrCode = null' PetSafetyApp.kt` must equal **1** (the `onQrCodeHandled` at `:313`); a second clear site turns the board red. *(A unit test here would be vacuous — you cannot test the absence of code that does not exist.)*
  - **C4 owns the close:** on the landing, when `savedQrCode != null` at appearance, present the Zone-1 scan surface **seeded** with `pendingQrCode = savedQrCode` (`LaunchedEffect(savedQrCode)` — auto-present, not tap-only). No `MainTabScaffold` touch, no VM change, no new component. **Requires an approved amendment to spec §E C4** (files / precise edit / done-when / test) before it is built.
  - **⚠️ Open C4 sub-decision — the premise is UNVERIFIED, do not rule on it yet.** It is claimed that presenting `QrScannerScreen` pre-seeded also fires a **camera-permission request** (`:144-145`) for a flow that needs no camera. **That claim is uncited** — nobody has read it (Rule 3: a report is not the code). **Read first, decide second:** (1) `QrScannerScreen.kt` ~`:140-155` — is the permission request unconditional on composition, or already keyed on something? (2) is the camera preview composed when `scanResult != null`? (3) with permission **denied** and `scanResult != null`, does the sheet still present? *(It should — `scanResult` is written from a `String` (`:65`), never from a frame — but if a permission-error state swallows the screen, a finder who taps "Don't allow" sees nothing at all.)* (4) **what does an *authed* deep-link user get today?** `:148-151` already runs this exact path in production, so the answer falls out of (1)–(3) and is load-bearing: if they already eat the prompt, C4 at parity introduces no regression; if they don't, C4 would *introduce* one.
  - **If the prompt turns out to be real, the likely answer is NOT "accept it."** Asking a stranger who just found a lost dog for **camera access**, in order to render a page that never touches the camera, is a defect at the exact moment the product exists for — and "Don't allow" is a completely reasonable tap. **Guard the request on `pendingQrCode == null`:** tap the Scan tab → request as today; arrive via a seeded code → no camera needed, no prompt. One conditional, and **it fixes the authed deep-link path too**, which has the identical wart right now. *(The "lighter host" alternative is **dead**: `ScannedPetSheet` is `private` (`QrScannerScreen.kt:454`), so a separate host cannot render it without de-privatising it and editing a Scan-tab file anyway — a new component (G-b) and a wider blast radius, worse on every axis.)* **Open question if guarded:** dismissing the sheet leaves the user on a scanner that never requested permission — something must ask before a *second* scan. Read (2) settles it.
  - **Done-when:** logged-out cold-launch tag scan → landing → public pet profile surface, **device-verified** (Rule 5; the simulator cannot do this).
  - *(Note: `lookupAndRoute` also fires `repository.scanQr(code)` at `:68-69` — correct for a logged-out finder. A logged-out **owner** scanning their own tag lands on the finder-facing page and notifies themselves — the same defect as **G-owner**, reached by a second path.)*
- **G12 — iOS cold-launch universal link has no handler above the splash gate** *(new 2026-07-12; **confirmed on `origin/main` = production**).* Both `.onOpenURL` handlers are inside `ContentView` (`PetSafetyApp.swift:180` on the `else`/`showSplash==false` branch + `ContentView.swift:102`); `ContentView` doesn't exist while `showSplash == true`, and production's splash is `asyncAfter(0.8)`. So a cold-launch `senra.pet/t/{id}` arriving during the splash lands in a hierarchy with **no `.onOpenURL`** — whether SwiftUI replays it on `ContentView` appearance or **drops it** is version-dependent and needs a 2-min cold-launch QA to settle. If it drops, it's a **live production defect** hitting real finders scanning tags. `b3f0fcf` (feat, unmerged) widens the window 0.8→2.0s; it reaches production only on redesign merge.
  - **Fix location (decision):** it's a defect on `main`, so the fix is a **standalone commit on `main`** — add an `.onOpenURL` capture **above** the `showSplash` gate → store into `@State` → consume when `ContentView` appears (mirroring Android's `pendingQrCode`), shipping in the next release; **C1 rebases onto it and stays a pure routing chunk.** Do **not** fold a production hotfix into C1 — a C1 revert must not also revert the hotfix.
  - **⚠️ Capture ≠ delivery — scope guard.** The fix **captures** the URL and preserves it for **post-login delivery** (to the authenticated `MainTabScaffold`, exactly as today) — strictly better than dropping. It does **NOT** attempt **logged-out delivery**. "Which public surface shows a tag deep-link when logged out" is a **Q-level decision** that would wire the **dormant iOS `ScannedPetView`** (§6-G7), which the handover says **do not wire**. That belongs to Viktor and to **Phase 2** (the scan surfaces), not here. **Do not wire `ScannedPetView` to close G12.**
  - **QA (both platforms):** cold start from a tag URL, app installed → land on the pet page, not landing/home.
  - **✅ FIXED 2026-07-14 — `56db26f` on `fix/deeplink-root-handler`** (branched off `origin/main`, worktree `../senra-deeplink`; **committed and pushed, not yet merged**). One `.onOpenURL` hoisted to a **`ZStack`** at the `WindowGroup` root, mounted from frame one, delegating to both handlers; the two inner ones deleted. **No dedup window** (the handlers are disjoint on `url.host`). See **§9.8** for why the `ZStack` is load-bearing and why a `Group` is not. **The real-device cold-launch QA is still owed** — it is the only proof the bug is dead.

**Gaps that were being tracked outside this register (folded in 2026-07-14 — they had no home and were drifting):**

- **G-owner — `lookup.isOwner` is parsed, logged, and never branched on** (`DeepLinkService.swift:154`). An owner scanning **their own tag** lands on the **public finder-facing pet profile** *and* fires the fire-and-forget `scanQRCode` call — logging the scan and sending themselves a *"your pet was scanned"* notification about their own pet. **Every new customer who tests their tag on arrival hits this.** Android reaches the same defect through `lookupAndRoute` → `scanQr` (`QrScannerViewModel.kt:68-69`). **Strong candidate for a small standalone fix.** Mechanised in `senra-status.sh` §3.
- **G-pet — `senra://pet/{petId}` returns `true` and does nothing** (`DeepLinkService.swift:91-100`). A handler reporting *"handled"* while dropping the URL — **the same failure mode PR #39 existed to fix, one branch over.**
- **G-doc — `DeepLinkService`'s header comment (`:4-7`) advertises only `/qr/{code}`**; the parser also handles `/t/{code}` (`:120`). Rule 3 in miniature: the comment is a claim, and it is wrong.
- **G12b — Where a logged-out tag deep link should land was recorded as a Q-level Phase-2 decision.** ✅ **Answered by the G11 trace (2026-07-14):** iOS already answers it (container-level sheet, no auth branch); Android answers it in **C4** via the seeded scan surface. **`ScannedPetView` (iOS, dormant) stays unwired** — it was never the route.
- **G-guard — a scope guard that names a file does not guard a behavior** *(process gap, new).* G12b forbade wiring `ScannedPetView`. Nobody wired it. **Logged-out delivery shipped on iOS regardless**, via a live component one branch over. Guards must be written against the **behavior** ("no logged-out delivery of a tag scan") and, where possible, **mechanised in `senra-status.sh`** — a prose prohibition naming a filename is unenforceable and gives false comfort.
- **G-home — Q6's text says the doc home is the repo ROOT; the board and this log say `docs/`. The text is the stale one. Viktor's ruling owed** *(surfaced 2026-07-17 by CC; documented, NOT silently fixed).* Three artifacts disagree about where the governing docs live, and they were reconciled by moving files, so the contradiction is now only in the prose:
  - **§7-Q6 (2026-07-09)** — *"Single home at `pet-safety-ios/SENRA-MOBILE-REDESIGN.md`"* → **repo root**.
  - **This change log (2026-07-14)** — *"Documentation consolidated … `docs/PROTOCOL.md` … `docs/HANDOVER.md` … `scripts/senra-status.sh`. **Docs move to `main`; `docs/mobile-redesign` retires.**"* → **`docs/` + `scripts/`**.
  - **`senra-status.sh` (2026-07-17, newest)** — `PLAN="$IOS/docs/SENRA-MOBILE-REDESIGN.md"` (`:21`), and §9 `fail`s unless `docs/{SENRA-MOBILE-REDESIGN,phase-1-spec,PROTOCOL,HANDOVER}.md` + `scripts/senra-status.sh` are **tracked** (`:206-212`) → **`docs/` + `scripts/`**.
  - **Two of three agree, and they are the two that are newer and executable.** `e81e5be` (2026-07-17) tracked the docs at `docs/` + `scripts/`, matching the board and the consolidation entry, and moved `SENRA-MOBILE-REDESIGN.md` root→`docs/`. **Q6's wording was superseded on 2026-07-14, not by that commit.** It is recorded here rather than edited in place because **Q6 is Viktor's, and a resolved question is not CC's to rewrite** — precisely the drift-by-restatement this register exists to catch (§1: one owner per fact).
  - **Owed:** either amend §7-Q6's text to `docs/`, or rule that root was correct and revert `e81e5be`'s move — in which case `senra-status.sh:206` and `:21` must change with it, or the board goes red on its own doc-home check.
  - **Also still false until the merge:** `PROTOCOL.md:3` and `senra-status.sh:4` both claim *"tracked on `main`"*. Both currently live on `docs/mobile-redesign`. The 2026-07-14 entry already commits to the merge; until it happens, two documents assert a location that is not true, and `scripts/senra-status.sh` cannot be run from a code branch at all.

---

## 7. Open questions / decisions pending

- **Q1 — RESOLVED 2026-07-09 → promoted to Locked decisions (§2).** Logged-out finders get exactly **Report-a-sighting + Scan-a-tag** on the missing-pet detail and nothing else; the detail becomes a public read with owner actions hidden when logged out. Implemented as sub-chunk **2.3b** (§4). (Recorded as a decision in §2, not left as an open recommendation here.)
- **Q2 — RESOLVED 2026-07-10 → outcome (A): anonymous-capable as-is, fully client-only** (§5.6). `POST /alerts/:alertId/sightings` is already `optionalAuth` with a nullable `reporter_id`, both client DTOs carry no user field, and a logged-out call is accepted — **no backend change**. 2.3b proceeds client-only; note the iOS-relocates / Android-builds-from-repo (per G6) asymmetry (§4 2.3b).
- **Q3 — Corrected OTP/account-created copy wording (HU canonical).** What should the Phase-3 success surface say? Must not claim a code was emailed. Needs the HU source string (HU is canonical) before EN + 12 locales are derived.
- **Q4 — RESOLVED 2026-07-10: Android discards `response.message`, matches iOS** (§5.6). No live inaccurate mobile string today; the copy fix stays folded into the Phase-3.3 success surface (G4 unchanged).
- **Q5 — RESOLVED 2026-07-10 → Shape A (three-zone community home base)** (§2). Zone 1 Act-now (Scan + found-stray), Zone 2 distinct Order-a-tag CTA, Zone 3 data-driven Community list (Lost & Found + Pet-friendly, extensible), persistent quiet Sign-in/Register; guardrails G-a (no empty placeholders) + G-b (reuse §5.3 components). **Q3 remains the only open decision on the critical path.**
- **Q6 — RESOLVED 2026-07-09.** Single home at `pet-safety-ios/SENRA-MOBILE-REDESIGN.md` (per instruction). No mirror or symlink; Android refs are pathed from `pet-safety-android/` inside this doc.

---

## 8. Specifiable vs. blocked (Unit-0 close-out)

**Now specifiable (Phase 0 resolved them):**
- **Phase 1.1 / 1.2** (shell + splash + landing) — gate insertion points + reusable tokens/components mapped (§5.1, §5.3); landing content locked to **Shape A** (§2). Each splits **a = shell/routing + splash** (carries the logout/expiry acceptance checks) and **b = three-zone landing content** (Act-now / Order / data-driven Community list). This is the **heaviest Phase-1 work** and leans hardest on §5.3 components (G-b — the spec must name the Community-card components, or surface a gap first). iOS refreshes `SplashScreenView` + mirrors `WelcomeView`; Android builds both new. Critical-path open decisions: **Q3 only** (Phase 3.3).
- **Phase 2.1 (scan)** and **2.2 (found-stray)** — trivial, no authed deps; concrete file targets listed (§5.2). Includes the G7/G8 cleanups.
- **Phase 2.4 (pet-friendly)** — read decoupling + the single authed action (submit-place) are pinned; only the login-prompt UX detail remains (reuse deep-link-sheet pattern).
- **Phase 3.1 / 3.2 (guest-order wiring)** — exact edits known: capture `userId`/`email` from `createOrder`, extend `CreateTagCheckoutRequest` (`Order.swift:194` / `Requests.kt:279`), thread on guest path; backend already supports it (G5). No backend change.
- **Phase 2.3 (board) + 2.3b (detail)** — now specifiable and **fully client-only** (Q2 resolved → outcome A, §5.6; no backend change). iOS relocates `ReportSightingView`; Android builds the sighting entry from `AlertsRepository.reportSighting`, lifting from the dormant dialog per G6. Prerequisite: dormant-view deletion (G6).

**Still blocked / needs a decision before the chunk is final:**
- **Phase 3.3 (copy fix)** — blocked on **Q3** (HU canonical wording). *(Q4 resolved — §5.6/§7 — no live mobile string; the copy work stays folded into the Phase-3.3 success surface.)*
- **Phase 4** — deferred by scope (invoicing boundary); do not plan internals yet.

---

## 9. Build-time codebase-verified findings (living log)

> Opened **2026-07-12** at Phase-1 build kickoff. **Refreshed as each chunk is built** — records where the live codebase differs from what the Phase-0 plan/spec assumed, verified by reading the actual files (never trusting the docs). Newest notes first.

### 9.0 Git / branch state (verified 2026-07-12)
- **Billing/invoicing "C7" is now MERGED into `main`.** `main == origin/main == szamla/c7-tag-billing-address == origin/szamla/…`, all at **`6cbf6bb`** (fast-forward on `a942338`). The earlier "off-limits parallel workstream" has landed; building on `main` now includes it. Its **source files** (`OrderMoreTagsView`, `OrderReplacementTagView`, `APIService`, `Localizable.strings`) don't overlap any redesign C0–C4 **source** file — **but that is NOT zero overlap** (corrected 2026-07-12 per the C0-iOS review; the earlier "zero overlap, verified" claim was wrong). The redesign and invoicing **share the `PetSafetyTests` build target**, so an invoicing-only compile error blocks the redesign's tests from compiling/running — which is exactly what happened (billing C7 → stale `OrdersDeliveryMethodTests`, see **§9.5**). The overlap lives at the **shared test target**, not the file list. *(origin/main has since advanced to `bc090fc` — the §9.5 test fix, then refined to model billing = shipping; local `main` still lags at `6cbf6bb`.)*
- **Build branch = `feat/mobile-redesign-phase1`** (per repo), created off `origin/main` (`6cbf6bb`). **One reviewed commit per chunk** (iOS repo carries C0-iOS/C1/C3; Android repo carries C0-Android/C2/C4) — this **supersedes the earlier per-chunk-branch steer** in the handovers.
- `docs/mobile-redesign` had carried a **duplicate billing cherry-pick** (`253e9ff`) on top of the spec `ed418a6`. **Reverted in `a1f0f60`** (2026-07-12) — the branch tree is back to `ed418a6` (docs-only), and §9 now lives here as the canonical repo-root master (Q6, no mirror). Spec/plan content unchanged since `ed418a6`, verified.

### 9.1 Test harness — Swift Testing, not (only) XCTest ⚠️ corrects the spec/handovers
- The spec (§E) and both handovers say **"XCTest."** Actual `PetSafety/PetSafetyTests/`: **33 files use Swift Testing** (`import Testing`, `@Suite`, `@Test`, `#expect`) vs **7 files `import XCTest`**. New chunk tests should be authored in **Swift Testing** to match the dominant style.
- **No ViewInspector** dependency present. SwiftUI view internals (`.onAppear`, the body subtree, which `Image` is drawn) are **not introspectable** in unit tests → view smoke tests assert on **constructable API + stored closures + helper contracts**; rendered pixels and animation timing are **visual-QA** (exactly what the spec's C0 note anticipates). A thin **`PetSafetyUITests`** XCUITest target exists (2 files) for launch/E2E if ever needed.

### 9.2 Asset catalog is split (verified 2026-07-12)
- **Not** a single `Assets.xcassets`. Two catalogs under `PetSafety/PetSafety/Resources/`: **`Colors.xcassets`** (colorsets) + **`Images.xcassets`** (imagesets).
- Confirmed present: colorsets `BackgroundColor`, `BrandColor`, `BrandColorDeep`, `Cream`, `Ink`, `SoftBorder`; **13 `LogoNew_*`** imagesets (incl. the `LogoNew_EN` fallback); `LaunchLogo`.

### 9.3 C0-iOS contract sites (verified) — spec is accurate
- `Views/SplashScreenView.swift`: minimal — `Color("BackgroundColor")` + `Image(LocalizedLogo.imageName)` (`.resizable().scaledToFit()`) + `DispatchQueue.main.asyncAfter(0.8) { onFinished() }`.
- Gate/handoff owner: **`App/PetSafetyApp.swift:31`** `@State private var showSplash = true`; **`:127-132`** `if showSplash { SplashScreenView { withAnimation(.easeInOut(duration: 0.4)) { showSplash = false } } } else { ContentView()… }`. C0 must preserve both the 0.8s→`onFinished()` handoff and this gate.
- `LocalizedLogo.imageName` maps 12 language suffixes → `LogoNew_<SUFFIX>`, fallback `LogoNew_EN`. (`WelcomeView` uses a *different* hero asset, `LaunchLogo`.)

### 9.4 C1 gate line-drift (note for the C1 build)
- Spec §E C1 cites `ContentView.swift:15-33` for the gate. Actual: the auth branch `if authViewModel.isAuthenticated {` is at **`:16`**; `.animation(.easeInOut(duration: 0.3), value: authViewModel.isAuthenticated)` at **`:38`**; `isAuthenticated` deep-link-sheet reads at `:53` and `:84`. Structure matches the spec; **re-confirm exact lines when building C1** (numbers drifted slightly).

### 9.5 ✅ RESOLVED (was a BLOCKER) — `PetSafetyTests` target red on `main` (billing C7, invoicing territory)
- **Discovered** while running the C0-iOS smoke tests (2026-07-12). Billing/invoicing **C7** (`6cbf6bb`) added a **required** `let billingAddress: ShippingAddress?` to `CreateReplacementOrderRequest` (`Services/APIService.swift:1543`) but did **not** update `PetSafetyTests/OrdersDeliveryMethodTests.swift`, which constructed that type without the arg → `:195`/`:219` failed (`missing argument for parameter 'billingAddress'`) with a `:205` cascade, the whole `PetSafetyTests` target failed to compile, and **no test could run** (C0-iOS's three included). **Independent of C0-iOS** — `SplashScreenView.swift` and `SplashScreenViewTests.swift` both compiled clean in the same run (confirming the SourceKit errors were false positives).
- **CORRECT FIX — test-only, ZERO production lines. Landed in two steps:** `f48d122` first supplied `billingAddress: nil` at the two `OrdersDeliveryMethodTests` call sites; **`bc090fc` (2026-07-12 14:23) then superseded that to model billing = _shipping_.** **Why `= shipping` beat `nil`:** `nil` billing is **not a sanctioned production state** — C7's billing-primary design *always* collects billing, defaulting to the "same as shipping" tickbox, so a test asserting `nil` would model (and propagate) a state the app never produces; `= shipping` models the path C7 actually ships. *(For the record: the `bc090fc` refinement was the better call than the `nil` fix the review originally signed off on — recorded, not quietly improved.)*
- **⛔ Do NOT "fix" this by defaulting the model (`= nil` on `APIService.swift:1543`).** A `let` optional gets **no** synthesized memberwise default, and that omission is **deliberate**: it forces every call site to supply billing. A nil default would silently remove the **compile-time forcing function** that C7's **billing-primary / never-infer / gift-null → dead-letter** design depends on. Recorded here so nobody applies it later as a "cleanup." *(This reverses this section's earlier draft, which wrongly recommended the default — corrected per the C0-iOS review.)*
- **Invoicing boundary honored** — the redesign seat touched no invoicing file; the fix was Viktor's. C0-iOS tests can now execute on a green target.

### 9.6 C0-Android — built as the iOS mirror + review round (2026-07-12)
- **Mirror of the final C0-iOS design** on `feat/mobile-redesign-phase1` (Android): system splash made **bare** (`themes.xml` light+night → adaptive `background_*` + a transparent `splash_icon_transparent` suppressing the launcher icon); new **`ui/screens/SplashScreen.kt`** (the square `splash_mark` "X" mark, 0.32s bloom + soft brand glow, 2.0s hold); `ui/PetSafetyApp.kt` gains a **true splash gate**; new Robolectric/`compose.ui.test` `SplashScreenTest`. **Scope beyond spec §E** (which listed only the two theme XMLs) — recorded here; `PetSafetyApp.kt` overlaps **C2's** file (see the §E C2 re-base note).
- **Review-driven corrections:**
  - **True gate, not an overlay** — ViewModels/Hilt + the `pendingQrCode → savedQrCode` capture stay **above** the gate (init overlaps the hold; a cold-start tag link is captured, not swallowed — see **G11/G12**); only the UI is gated (`Crossfade { if (showSplash) SplashScreen else { Box { Scaffold + dialogs } } }`), so the app never composes under the splash. Session-expiry dialog surfaces **after** the gate (C2's `sessionExpiryRoutesToLanding` leans on this).
  - **Glow** = full-screen radial, explicit `radius = 340.dp.toPx()` (mirrors iOS `endRadius: 340`); fades to **`BrandOrange.copy(alpha = 0f)`** (transparent *orange*, not `Color.Transparent` = transparent black — which would muddy the fade through grey).
  - **Mark** — the provided `Logo_Android_new.png` was square but **opaque** (no alpha → white box in dark mode), so the transparent iOS `LaunchLogo` was ported to `drawable-mdpi/xhdpi/xxhdpi` as `splash_mark` (200/400/600), drawn **pixel-native at 200.dp** (was 240 → ~20% upscale) on **both** platforms (iOS `maxWidth: 200`).
  - **`HOLD_DURATION_MS`** → `internal`.
- **iOS amendments (same pass):** fixed the stale `// Handoff contract — unchanged` comment (hold went 0.8→2.0); `maxWidth 240 → 200`; **restored** the all-13-`LogoNew_*` existence check as a new **`LocalizedLogoTests`** suite (dropped from `SplashScreenViewTests` in round 2; the lockups are still live on content screens).
- **Reduce-motion parity:** iOS `accessibilityReduceMotion`; Android `Settings.Global.ANIMATOR_DURATION_SCALE == 0` (no direct equivalent) — a deliberate, documented divergence.
- **QA register (before release):** (1) **deep-link** cold-start from a tag URL on **both** platforms (G12); (2) **first-composition-in-fade** — the whole UI tree first composes at t=2.0s during the 400ms crossfade (under the old overlay it warmed up beneath the splash) → watch the handoff on a **low-end device**, not the emulator, for stutter/blank frame; (3) dark-mode mark strokes; (4) on-device system-splash icon suppression.

- **⚠️ C0-Android has NO hash-verified review trail — unlike C2 (recorded 2026-07-17).** Its build artifact was never hash-recorded, so no Rule 7 match was ever asserted for it. **`51b8965` is authoritative** — the commit is the only evidence of what C0-Android actually shipped. A loose `C0-Android.diff` (sha256 `da4deaa57a97`) sat untracked in the Android repo root until 2026-07-17 and was deleted: it was never reviewed against, never cited here, and **`da4deaa57a97` must NOT be read as a reviewed hash** — recording it as one would manufacture a review trail that does not exist. Contrast **C2-Android**, whose artifact `d753c01d17c4` *was* hashed, matched at commit, and is cited in §9.11. The asymmetry is the point: if C0-Android is ever audited, there is no artifact to re-hash, only `51b8965`.

### 9.7 C1-iOS as built — the routing seam (`9c50b25`, committed 2026-07-14, 19/19 green)

- **Files (7):** `App/RootRoute.swift` (new), `App/ContentView.swift`, `ViewModels/AuthViewModel.swift`, `Views/Auth/AuthenticationView.swift`, `Views/Auth/RegistrationView.swift`, `Views/Landing/LandingView.swift` (new), `PetSafetyTests/RootRouteTests.swift` (new). **`PetSafetyApp.swift` was NOT touched** — this matters for the merge (§9.8).
- **The seam.** `RootRoute { main, register, login, landing }` + `AuthOverlay { none, login, register }` — **both-true is unrepresentable by the type**, which is *stronger* than the spec's "mutual exclusion by clearing": you cannot fail to clear a flag that does not exist. `ContentView` **switches on `RootRoute.resolve(...)`**; the two `.animation(value:)` collapsed into one `value: route`; `overlay` resets to `.none` when `isAuthenticated` flips true (`staleOverlayDoesNotSurviveLogout`).
- **🔑 Boundary refinement (approved, and it governs C2/C3/C4):** ***`isAuthenticated`'s computation is untouchable. Supplying a dependency through a defaulted parameter is a seam, not a derivation change.*** The old file-level rule ("don't touch `AuthViewModel`") was a proxy for a logic-level rule, and the proxy blocked the very test the spec demanded. `AuthViewModel` gained four defaulted seams (`hasStoredToken` / `fetchCurrentUser` / `connectSSE` closures + an `authCheckTask` handle). **Zero production call-site changes; production byte-identical.** Mechanised in `senra-status.sh` §4.
- **Both auth surfaces need TWO exits.** C1 first shipped a *green* `backFromAuthReturnsToLanding` while the login screen had **no UI path that could trigger it** — a dead end hiding behind a passing test. The same bug then reappeared on register. **The rule: every auth surface needs a *lateral* switch AND a *back*.** Login has `onNavigateToRegister` + `onBack`; Register has `onBackToLogin` + `onBack`. Both `onBack` → `dismissAuth()` → landing.
- **Test seams beat global state.** The session-expiry test injects the gate (`hasStoredToken: { true }`) rather than writing a real keychain token. **`.serialized` alone was not enough** — Swift Testing parallelizes *across* suites, so a real token would have been visible to `AuthViewModelTests`' plain `AuthViewModel()` constructions → `Task { getCurrentUser() }` → **a live production network call with a bogus token.**
- **Deep-link sheets are container-level, not route-level** (verified 2026-07-14): all three `.sheet(isPresented: $deepLinkService.…)` hang on the `Group` at `ContentView.swift:42`, **after** the `switch` closes at `:41` — same modifier chain as `.onChange` (`:46`) and `.animation(value: route)` (`:52`). **None is inside a `case`.** Consequence: **the route switch cannot interfere with sheet presentation**, and a logged-out iOS user already gets `DeepLinkScannedPetView` with no auth branch (`:84`). See **G11**.

### 9.8 The deep-link root handler (`56db26f`) — and why `ZStack` is load-bearing

- **The bug (production).** Both `.onOpenURL` handlers lived inside `ContentView` (`ContentView.swift:102` → tag/QR; `PetSafetyApp.swift:180` → Stripe checkout return — **a different function of the same name**, `handleDeepLink`). `ContentView` **does not exist while `showSplash == true`**, so a cold-launch tag link had no handler and was dropped. C0 widened the window **0.8s → 2.0s**. The Stripe checkout return was being dropped in the same window.
- **Consumption was always sound** — `.sheet(isPresented:)` bound to `@Published` on the observed singleton, **no `.onChange`** — so a flag already true at mount presents the sheet. **Delivery was the broken link, not consumption.**
- **The first attempt used a `Group` and hung `.onOpenURL` on it. It built clean and silently killed the splash→content crossfade.** A physical device caught it; **no test could have.** *(This is Rule 5, and it is the single best example of it in this project's history.)*
- **The shipped fix uses a `ZStack`, and the choice is load-bearing.** A `ZStack` is a **real layout container with stable identity**; a `Group` is a type-erasing `@ViewBuilder` helper. Two consequences: (1) the `.transition(.opacity)` + `withAnimation(0.4)` ride the container's identity, so both branches coexist during the crossfade → **the fade works**; (2) `.onOpenURL` attaches **to the ZStack itself** rather than propagating to its members → **exactly one registration, always** — the 400ms double-fire is not improbable, it is **structurally impossible**.
- **⚠️ Merge shape — VERIFIED 2026-07-14, and the news is that it is CLEAN.** `git merge-tree feat/mobile-redesign-phase1 origin/fix/deeplink-root-handler` → **exit 0**, tree `6ca1e1ff`. Neither C0 commit (`b3f0fcf`, `679a8a1`) nor C1 (`9c50b25`) touched `PetSafetyApp.swift`, so the merged file is **byte-identical to the device-verified version**. Merged-tree counts: `ContentView.swift` `onOpenURL` = **0**; `PetSafetyApp.swift` `onOpenURL` = **1**, `ZStack` = **2**. Textually correct.
- **🔴 What a clean merge costs: nobody is forced to look.** The `ZStack` was device-verified against a **pre-C1 `ContentView`**. Post-merge, `ContentView` carries C1's `.animation(.easeInOut(duration: 0.3), value: route)` (`:52`) — a **second** animation that can fire at the exact moment the outer 0.4s crossfade runs and `ContentView` composes for the first time. **The case to look at is a cold launch while LOGGED IN**, not logged out: with a stored token, `isAuthenticated` flips async, so `route` transitions `.landing → .main` *inside* the crossfade window — two nested animations on first composition. Logged out, `route` is `.landing` from frame one and nothing animates. **This is every returning user's cold launch, not an edge case.** *(HYPOTHESIS on the exact timing of the optimistic flip — but the QA case is cheap and the failure mode is visible.)* **Build ≠ run ≠ look.**

### 9.9 Android auth stack (verified 2026-07-14) — Hilt makes C1's seam surgery unnecessary

**C2 needs NO seam work on `AuthViewModel`. The spec's "Must NOT touch" is satisfiable as literally written.**

- **Full constructor injection.** `AuthViewModel.kt:28-33` — `@HiltViewModel class AuthViewModel @Inject constructor(application, authRepository, fcmRepository)`; `AuthRepository.kt:37-42` takes `(apiService, tokenStore, fcmRepository?, offlineDataManager?)`. **There is no analogue of the iOS `AuthViewModel()`-with-no-DI that fired `getCurrentUser()` at production.**
- **`isAuthenticated` is a pure reactive derivation, not optimistic.** `AuthRepository.kt:44` — `val isAuthenticated: Flow<Boolean> = tokenStore.authToken.map { !it.isNullOrBlank() }`; collected at `AuthViewModel.kt:74-75`. `loadCurrentUser` failure **only logs** (`:306-309`) and never flips `_isAuthenticated`. **Demotion happens only when a token is actually cleared** — so iOS's "mid-session optimistic bounce" **has no Android analogue.** Android's expiry is simply *"expiry clears the token → `isAuth=false`."*
- **The expiry path already ships a production-safe test hook.** `TokenAuthenticator.kt:78-81` — `@VisibleForTesting(NONE) internal fun emitExpiryForTest()`, plus `resetExpiryGuard()` (`:70-72`); the event is a static `SharedFlow` (`:60-61`). **Real path, two-ended:** `clearAndNotify()` (`:213-217`) emits `_authExpiredEvent` once ↔ consumed at `AuthViewModel.kt:87-92` → `logout(TOKEN_EXPIRED)` + `_isAuthenticated = false` + `_sessionExpiredEvent.emit(…)`.
- **`logout()` is hermetic by construction.** `AuthViewModel.kt:171-181` → `AuthRepository.kt:90-118 logout(USER_INITIATED)`, whose only network reach is `unregisterFCMToken()` (`:95` → `:138` `fcmRepository?.removeToken()`), **guarded by the nullable `fcmRepository`.** Pass `null`/fake → no network; everything else is local prefs + local Room.
- **⚠️ Easy is when a verbatim criterion quietly shrinks.** A test that calls `emitExpiryForTest()` and then asserts `resolveRootRoute(false, NONE) == LANDING` has proved **that a pure function returns what its `when` says. It has proved nothing about expiry.** The test must ride the **real** path — `emitExpiryForTest()` → `_authExpiredEvent` → `AuthViewModel:87-92` → `_isAuthenticated = false` → *then* resolve — and it must preserve the **one-shot guard** (`resetExpiryGuard()`) and the dialog's **post-gate** surfacing (C0 put the dialog inside the gate).
- **The routing flags are NOT in the auth layer.** `screenKey` / `showOrderTagsScreen` / `showRegisterScreen` live in `PetSafetyApp.kt`. So the `RootRoute.kt` seam and the four-state `AuthOverlay` (incl. `ORDER_TAGS`) are **entirely in `PetSafetyApp.kt` + the new file** — cleanly separable from auth derivation, exactly as the spec assumes.

### 9.10 Process — where the documentation itself failed (2026-07-14)

- **§9 went two days and two chunks stale.** C1 (`9c50b25`) and the deep-link fix (`56db26f`) were committed, reviewed, and *not logged here*. A new session reading §9 would have been told C1 was unbuilt. **The CODEMAP is the answer to "reading is expensive" — it only works if it is refreshed per chunk, and it wasn't.** Now mechanised: `senra-status.sh` **fails the board if the branch tip's SHA does not appear in this document.**
- **The same fact lived in four documents, so it drifted in four documents.** "Rule 5" meant two different things; the docs tip had three different values; the gaps register was split across a plan, a tracker, and a handover. **Fixed by ownership:** one owner per fact (`docs/PROTOCOL.md` §1), handovers contain **no facts**, and **the board is a script, not a table.**
- **The drift detector was itself drifting.** `senra-status.sh` v1's contract check grepped the spec for prose (`holdDuration.*2.0`) that appears **zero** times — it **false-fired against correct code.** A check that cries wolf trains you to skim past red. **Fixed** by §10 below: contracts are *declared*, then checked against source.

### 9.11 C2-Android as built — the routing seam mirrored (`6263693`, committed 2026-07-14, 797/0)

> Committed against the hash-verified tree **`d753c01d17c4`** (Rule 7 — re-hashed after a stash/pop, matched). 9 files, 647+/26−. Device-verified: both back chevrons, logout → landing, the fade into LANDING.

- **Files (9):** `ui/RootRoute.kt` (new), `ui/screens/LandingScreen.kt` (new), `ui/PetSafetyApp.kt`, `ui/AuthScreen.kt`, `ui/RegisterScreen.kt`, + tests `RootRouteTest.kt` (new, 13), `RootRoutingComposeTest.kt` (new, 2), `AuthBackAffordanceTest.kt` (new, 2), `AuthViewModelTest.kt` (+2). Reviewed artifact hash **`d753c01d17c4`**; re-hashed at commit after a stash/pop — matched.
- **🔑 THE cross-platform lesson — copy-on-write is not a style choice, it is the difference between working and dead.** iOS's `RootNavState` is a `struct` in `@State` with `mutating func`s; **SwiftUI republishes on in-place mutation. Compose does not.** `mutableStateOf` recomposes only when `.value` is **REASSIGNED** (structural equality). A class whose methods mutated an internal field would leave `.value` untouched → **no recomposition → tapping "Sign in" on the landing does nothing → and every pure value test still passes.** So `RootNavState` is a `data class`, every transition returns `copy(...)`, and **every call site is `nav = nav.enterX()`**. Guarded by `RootRoutingComposeTest.landingSignInRecomposesToLogin`, which **fails in the broken world while all 13 pure tests stay green.** *(This was caught in design review, before code existed. It would have been near-invisible after.)*
- **Four overlay states, not iOS's three.** Android has a pre-auth **order-tags** path iOS lacks. Rather than leave it as a loose `Boolean` beside the enum, `AuthOverlay { NONE, LOGIN, REGISTER, ORDER_TAGS }` makes it first-class — so "two overlays at once" stays **unrepresentable**, which is stronger than mutual-exclusion-by-clearing.
- **The two-exits scar, closed on Android.** `AuthScreen` had **no back at all** (it was the default); `RegisterScreen` had only the lateral switch. Post-C2 both are overlays above the landing, so both gained a real chevron → `onBack` → `dismiss()`. `.statusBarsPadding()` on both — the activity is `enableEdgeToEdge()`.
- **`AuthBackAffordanceTest` is the pattern: test the AFFORDANCE, not the closure.** It renders the **real** `AuthScreen`/`RegisterScreen` (mocked repos, real `AuthViewModel` — **no Hilt required; both screens take their VMs as plain parameters**, verified by grep: no `hiltViewModel()`, no `@Inject`, no `EntryPoint`) and **presses the real chevron** via `onNodeWithContentDescription`. **A closure-only assertion is the C1 dead-end** — C1 shipped a *green* `backFromAuthReturnsToLanding` over a login screen with no UI path that could trigger it, twice. **This is now the required shape for any "does backing out work" test.**
- **⚠️ It was nearly missed.** The first C2 artifact (`e8d836cfde6e`) was 17 tests and 795 green — and **`backFromAuthReturnsToLanding` was absent by name**, while the build report asserted every decision honored. `grep -c` on the artifact found it in seconds. **A named verbatim criterion is worth exactly one grep. Run it every chunk.**
- **Acceptance tests ride the real path.** `logoutRoutesToLanding` / `sessionExpiryRoutesToLanding`: the `false` fed into `resolveRootRoute` is the **observed** `viewModel.isAuthenticated.value` after the transition, **never a literal**. Expiry extends the existing `:480-499` test (`resetExpiryGuard()` → `emitExpiryForTest()` → real collector) rather than replacing it. *Recorded fragility:* the `@Before` VM stays subscribed to the **companion-level** `authExpiredEvent`, so two collectors fire `logout(TOKEN_EXPIRED)`. Harmless today (no count assertions, by convention); it would flake a future `coVerify(exactly = 1)`.
- **Preserved:** `MainTabScaffold`, auth derivation, the order flow (only its branch *selection* moved), the C0 splash gate, the session-expiry dialog (still inside the gate), `savedQrCode` (byte-identical — G11 is C4's). **Device-verified:** both back chevrons, logout → landing, the fade into LANDING.
- **⚠️ Debug-only.** The whole suite is `testDebugUnitTest`. **Release has never run this code under R8.** Minification will not break an enum in a `when` — but `assembleRelease` *succeeding* is not a minified build *running*, and release is what ships. Pre-release QA item.

### 9.12 🔴 The false-evidence pattern — four for four in one session (2026-07-14)

**Every scary artifact this session pointed at code that was correct.** In each case the tell was identical: **the evidence named a subsystem nobody had touched.**

| What it looked like | What it was | Cost |
|---|---|---|
| A corrupted hostname literal in a Kotlin `setOf` | The **chat channel** linkifying a bare hostname in transit | A confident, wrong, four-file bug report. **Withdrawn.** |
| `Fatal signal 6 (SIGABRT)` — "the app won't build" | The **system Bluetooth process** (`com.google.android.bluetooth`) crashing. Not Senra. Ambient emulator noise. | An hour chasing a phantom |
| `INSTALL_BASELINE_PROFILE_FAILED` — "the build is broken" | A **release-variant startup-profile installer step.** The APK had already compiled, assembled, R8'd, and shipped to the device. | Wrong variant, wrong hunt |
| **"The app shows the old login screen"** — "the seam is broken" | A **stale APK.** `lastUpdateTime` proves the *install* is fresh; it does **not** prove the *APK* is. The seam was correct the entire time. | Most of an evening |

**The rule, generalised from Rule 4:** *before debugging a line, check whether the evidence is even inside the blast radius.* An error naming a subsystem the change never touched is a claim about the **environment**, not the code — until proven otherwise.

**Practical corollary, now standing policy:** when the **device contradicts the source**, `adb uninstall` + `./gradlew clean` **before** you debug anything. Debug and release share `applicationId` (only `staging` carries a suffix) but are signed with **different keys** — so a stale install can survive and be relaunched, looking exactly like a code bug.

**And the counterweight, so this isn't read as "distrust everything":** the same session's discipline caught the one thing that *was* real — a missing verbatim acceptance test hiding behind 795 green. **The noise was in the environment. The signal was in the artifact. Hash the artifact; distrust the environment.**

---

## 10. Machine-checked contracts

> These are read by `scripts/senra-status.sh` §5 and compared **against the source**. Prose drifts silently; a declaration gets checked. **Change the code → change the line below, or the board goes red.**

<!-- CONTRACT: ios.splash.holdDuration = 2.0 -->
<!-- CONTRACT: android.splash.holdDurationMs = 2000 -->

| Contract | Value | Source of truth | Why it is pinned |
|---|---|---|---|
| `ios.splash.holdDuration` | `2.0` | `Views/SplashScreenView.swift` | C0 round 2 deliberately superseded the original `~0.8s → onFinished()` contract (Viktor's product call). The splash is now the app's single branded moment. **This is also the window in which cold-launch deep links were being dropped** — see §9.8. |
| `android.splash.holdDurationMs` | `2000` | `ui/screens/SplashScreen.kt` (`HOLD_DURATION_MS`) | Cross-platform parity with the above. |

**Not yet mechanisable, and therefore still on the board as ⚠️, not ✅:** the merged `WindowGroup` crossfade (device), low-end first-composition-in-fade (device), dark-mode mark strokes (device), OEM splash-icon suppression (device), real-tag cold-launch delivery (device + a physical tag). **A build is not a run, and a run is not a look.**

---

## Change log
- **2026-07-09** — Unit 0: doc created; Phase 0 traces 0.1–0.5 run and recorded (§5); gaps register seeded + G6/G7/G8/G9 added from findings (§6); open questions Q1–Q6 opened (§7); specifiable/blocked close-out (§8). No feature code, no branch. Surfaced for review; not yet committed (Viktor owns git).
- **2026-07-09 (rev 2, post-review)** — **Q1 resolved and promoted to Locked decisions (§2)**: logged-out finders get only Report-a-sighting + Scan-a-tag on the missing-pet detail; folded into §4 (2.3 + new sub-chunk 2.3b), §7-Q1 (marked resolved, recommendation removed), and §8. Added **logout/session-expiry → landing acceptance check** to the 1.1/1.2 done-definitions (§4), incl. the iOS mid-session optimistic-bounce case. Added **2.4 cross-platform `market` device-locale fallback parity note** (§4). **Q6 resolved** (single doc home, no mirror). No feature code, no branch. Re-surfaced for review; not committed.
- **2026-07-10 (rev 3, post-review)** — Q2 + Q4 read-only checks run and recorded (§5.6). **Q2 RESOLVED → outcome (A)**: report-a-sighting is anonymous-capable as-is (`optionalAuth` route + nullable `reporter_id` + user-less client DTOs), so 2.3b is **fully client-only** — no backend sub-unit spawned; §4 2.3b now records the **iOS-relocates / Android-builds-from-repo-per-G6** report-sighting asymmetry. **Q4 RESOLVED**: Android discards the backend order `message`, matching iOS — no live inaccurate mobile string, G4 stays latent. Updated §4 (2.3b), §5 (new §5.6), §7 (Q2/Q4), §8. No feature code, no branch, no build. Surfaced for review; not yet committed.
- **2026-07-10 (rev 4)** — **Q5 RESOLVED → Shape A** recorded as a locked decision (§2): landing = three-zone "community home base" (Zone 1 Act-now: Scan + found-stray; Zone 2 distinct Order-a-tag CTA; Zone 3 **data-driven, extensible** Community list; persistent quiet Sign-in/Register), with guardrails **G-a** (no empty "coming soon" placeholders) + **G-b** (Community cards reuse §5.3 components — named in the spec or surfaced as a gap first). Folded the three zones + an **a/b split steer** (a = shell/routing + splash carrying the logout/expiry checks; b = landing content/layout) into §4 1.1/1.2; §7-Q5 marked resolved; §8 Phase-1 bullet updated. **Q3** is now the only open decision on the critical path. Doc-only; no feature code, no branch, no build. Surfaced for review; not committed.
- **2026-07-12 (Phase-1 build kickoff)** — Created build branch **`feat/mobile-redesign-phase1`** off `origin/main` (`6cbf6bb`; billing/invoicing C7 now merged to `main`). Reconciled git state and opened **§9 build-time codebase-verified findings** (Swift Testing harness — *not* XCTest; no ViewInspector; split `Colors`/`Images` xcassets; C0/C1 contract sites verified). Branch convention changed to **one per-repo Phase-1 branch, one reviewed commit per chunk** (both handovers updated).
- **2026-07-12 (C0-iOS build + review round 1)** — Built **C0-iOS**: a pure-visual splash refresh in `SplashScreenView.swift` (soft brand glow + fade/scale entrance + Reduce-Motion guard; the `BackgroundColor` / `LocalizedLogo` / `0.8s → onFinished()` / `showSplash`-gate contracts all preserved) plus a 4-test Swift Testing smoke suite. Running it surfaced a **blocker (§9.5):** billing C7 had left `OrdersDeliveryMethodTests` stale, so the shared `PetSafetyTests` target was red — which **falsified the earlier "zero overlap" claim (§9.0 corrected):** the overlap is the shared test target, not the file list. **Resolved** test-only by Viktor (`f48d122`, then refined in `bc090fc` to model billing = shipping — see §9.5; **not** the model `= nil` default, which §9.5 explicitly forbids and explains). C0-iOS **review = CHANGES REQUESTED**; all addressed: §9.0/§9.5 corrections; splash `maxWidth 320 → 360` (binds on iPad only — no current iPhone binds) and entrance `0.5s → 0.32s` (a settled dwell, not a flicker); honest test names/comments + an all-13-`LogoNew_*`-asset existence check (with the inconclusive-on-`private`-mapping limitation recorded); the duplicate billing cherry-pick reverted off `docs/mobile-redesign`; and **§9 ported to this canonical repo-root home with the `docs/` mirror deleted (Q6 upheld, not amended).** C0-iOS visual QA (Xcode canvas — 3 device classes × light/dark, the §E C0 done-when gate) + commit **pending; not yet committed.**
- **2026-07-12 (C0-iOS round 2 — COMMITTED `b3f0fcf`)** — After visual QA, C0-iOS evolved (Viktor-directed) from a pure `SplashScreenView` refresh into a **launch-screen + splash redesign**, committed on `feat/mobile-redesign-phase1` (3 files). This **expands the C0-iOS file set beyond spec §E's single `SplashScreenView.swift`** — recorded here as the decision. What shipped:
  - **`LaunchScreen.storyboard`** — removed the `LaunchLogo` imageView + constraints; the launch screen is now just the dark-adaptive `BackgroundColor`. This eliminated the real problem (a centered square mark on the launch screen *jumping* to a wide localized wordmark on the splash) **and** the fade-from-zero "flicker" — both gone because the launch screen no longer shows a logo. (`LaunchLogo` the asset stays — `WelcomeView` uses it.)
  - **`SplashScreenView.swift`** — the splash is now the app's single logo moment: the plain **`LaunchLogo` "X" mark** (language-neutral, no wordmark; the localized `LogoNew_*` lockup stays on the content screens), `maxWidth 240`, hold **2.0s**, 0.32s bloom + soft glow/shadow, Reduce-Motion guarded. *(Supersedes round-1's intermediate `LocalizedLogo` lockup + `maxWidth 320→360` + `0.8s` hold.)*
  - **Tests** refocused onto the mark (`LaunchLogo` existence + `holdDuration == 2.0`); the all-13-`LogoNew_*` check was dropped as no longer splash-relevant.
  - **Deferred / noted:** the launch-screen *duration* is cold-start init cost (Firebase/Sentry/App Check before the first frame) — shortening it is startup optimization outside C0. **Dark mode:** the mark isn't recolored for dark (matching the brand's own dark lockup), so its darker strokes are lower-contrast on the dark splash bg — a possible future dark-mark asset.
- **2026-07-12 (C0-Android + cross-platform review round)** — Built **C0-Android** as the iOS mirror (bare system splash + `SplashScreen.kt` mark + true `Crossfade` gate + `SplashScreenTest`) and drove it through review (**§9.6**): true-gate (ViewModels/Hilt + deep-link capture above the gate), glow `radius=340.dp` fading to transparent *orange*, transparent square `splash_mark` (the iOS `LaunchLogo` ported — the provided `Logo_Android_new.png` lacked alpha), pixel-native **200.dp** mark on both platforms, `HOLD_DURATION_MS` internal. **iOS amendments:** stale handoff comment fixed, `maxWidth 240→200`, `LocalizedLogoTests` restored. **Measured cold-start** (debug/emulator ~1.2s to first frame) → the "~5s" launch claim is **not real**; C0's shape holds. New gaps **G11** (pending scan stranded on a logged-out gate-drop) + **G12** (iOS cold-launch deep-link may drop during the splash — fix in C1). **Re-based §E C2's snippet** (the `screenKey` block moved to `:294-299` inside the C0 gate). QA register recorded. C0-Android + iOS amendments **pending commit**.
- **2026-07-12 (C0 committed; G12 decided; C1/C2 seam amendment)** — **Committed:** C0-iOS `b3f0fcf`+`679a8a1`, C0-Android `51b8965`. **G12** decided **separate** (standalone `.onOpenURL`-above-the-gate hotfix on `main`, C1 rebases; fix-regardless + a before/after cold-launch QA); capture≠delivery guard logged (do not wire `ScannedPetView`). **§E C1/C2 amended (approved):** the routing gate is extracted into a pure **`RootRoute.resolve(...)`** + an **`AuthOverlay`** enum that makes both-true **unrepresentable** (not merely cleared) — required because §9.1 (no ViewInspector) makes an inline `else if` chain untestable; iOS overlay `{none,login,register}`, **Android `{none,login,register,orderTags}`** (four-branch, the order path is a first-class state, not a loose Bool). Animations collapse to one `.animation(value: route)`. A **fifth test** falls out — `staleOverlayDoesNotSurviveLogout` (register→authed→logout must route to landing, not the stale register overlay) — the bug an inline chain hid. Adds `RootRoute.swift`/`RootRoute.kt` to the C1/C2 file sets (extracts *branch selection* only; auth/session derivation untouched).
- **2026-07-13/14 (C1-iOS built, reviewed, COMMITTED `9c50b25`)** — **§9.7.** The routing seam: `RootRoute.swift` (new) makes both-true **unrepresentable**; `ContentView` switches on `resolve(...)`; animations collapsed to one `value: route`; overlay resets on authenticate. `LandingView` (new, minimal scaffold, no placeholders — G-a). **Boundary refinement approved and now governing:** *`isAuthenticated`'s computation is untouchable; supplying a dependency through a defaulted parameter is a seam, not a derivation change* — `AuthViewModel` gains four defaulted test seams, **zero production call-site changes, production byte-identical** (mechanised: `senra-status.sh` §4). **Both auth surfaces gained a second exit** after C1 first shipped a *green* `backFromAuthReturnsToLanding` over a login screen with **no UI path that could trigger it** — a green test over a broken app; the same bug then reappeared on register. 19/19 green. `PetSafetyApp.swift` **not** touched.
- **2026-07-14 (deep-link root handler COMMITTED `56db26f`; PR #39 closed by replacement)** — **§9.8.** One `.onOpenURL` hoisted to a **`ZStack`** at the `WindowGroup` root, mounted from frame one; the two inner handlers deleted; no dedup window. **The first attempt used a `Group`, built clean, and silently killed the splash crossfade — caught only by a physical device.** Lives on `fix/deeplink-root-handler` off `origin/main` (worktree `../senra-deeplink`), **pushed, not yet merged.** **Merge shape verified:** merges **cleanly** into `feat/mobile-redesign-phase1` (tree `6ca1e1ff`; `ContentView` `onOpenURL`=0, `PetSafetyApp` `onOpenURL`=1, `ZStack`=2). **A clean merge means nobody is forced to look** — and the merged `WindowGroup`+`ContentView` pairing has **never run on a device**. QA case: **cold launch while LOGGED IN** (route flips `.landing→.main` inside the 0.4s crossfade). Still owed: the real-device, real-tag, cold-kill deep-link QA — the only proof the bug is dead. **Android App Links CLOSED:** `assetlinks.json` fingerprint is byte-identical to the Play **app-signing** key (not the upload key); note a *debug* build will not verify.
- **2026-07-14 (Android auth stack read; G11 resolved; documentation consolidated)** — **§9.9:** Hilt gives full constructor injection, `isAuthenticated` is a **reactive** derivation (not iOS's optimistic bounce), `TokenAuthenticator` already ships `emitExpiryForTest()`, and `logout()` is network-free with a null `fcmRepository` → **C2 needs no seam surgery; "Must NOT touch" is satisfiable as written.** **G11 RESOLVED → closes in C4** (§6): `savedQrCode`'s sole consumer is auth-gated (`PetSafetyApp.kt:312`), but `scanResult`'s sole writer `lookupAndRoute(code: String)` is **already driven by a raw code** from the deep-link path — and **spec §C.1 already requires C4 to present `QrScannerScreen` from the landing.** iOS has been shipping logged-out delivery all along (container-level sheets, `ContentView.swift:42/:80/:84`) — **this was a PARITY gap, not an open question.** New gaps folded into §6: **G-owner**, **G-pet**, **G-doc**, **G12b** (answered), **G-guard** (*a scope guard that names a file guards nothing*). **§10 machine-checked contracts** opened. **Documentation consolidated (§9.10):** five handovers + a tracker + a protocol → `docs/PROTOCOL.md` (one owner for the rules), `docs/HANDOVER.md` (**contains no facts**), this plan (the CODEMAP), the spec (the chunk contract), and `scripts/senra-status.sh` (**the board — derived, never written**). Docs move to `main`; `docs/mobile-redesign` retires. **`senra-status.sh` now fails if the branch tip has no entry in this change log.**
- **2026-07-14 (C2-Android built, reviewed, device-verified — COMMIT `6263693`)** — **§9.11.** The routing seam mirrored on Android: `RootRoute.kt` (new) with a **four**-state `AuthOverlay` (Android's pre-auth order path becomes first-class, not a loose `Boolean`); `screenKey` + both bools deleted; `resolveRootRoute` drives `AnimatedContent`. **`RootNavState` is COPY-ON-WRITE, and this is the chunk's real lesson:** SwiftUI republishes on in-place mutation, **Compose does not** — a mutating class would leave `mutableStateOf.value` untouched, so "Sign in" would do nothing **while every pure test stayed green**. Caught in design review, guarded by `RootRoutingComposeTest.landingSignInRecomposesToLogin`. **Two-exits scar closed on Android** (`AuthScreen` had no back at all): both surfaces gained a real chevron, and **`AuthBackAffordanceTest` presses the REAL chevron on the REAL screens** — no Hilt needed; a closure-only assertion *is* the C1 dead-end. **The first artifact (`e8d836cfde6e`) shipped 795 green with `backFromAuthReturnsToLanding` MISSING BY NAME** while the report claimed every decision honored — found by `grep -c` in seconds; amended to **`d753c01d17c4`**, re-hashed after a stash/pop, matched at commit. `savedQrCode` byte-identical (G11 is C4's). **§9.12 opened — the false-evidence pattern:** four artifacts this session (a linkified literal, a Bluetooth `SIGABRT`, a baseline-profile installer, a stale APK) all pointed at correct code, and **every one named a subsystem nobody had touched.** *Before debugging a line, check whether the evidence is even in the blast radius.* **G11's camera premise WITHDRAWN as uncited** (§6) — read `QrScannerScreen.kt:140-155` before ruling; the likely answer is to guard the permission request on `pendingQrCode == null`, which also fixes the authed deep-link path.
- **2026-07-17 (docs given a tracked home; both phase1 branches pushed; Android pointed at the docs — CC under §2 exception)** — **The governing docs were untracked.** `HANDOVER.md`, `PROTOCOL.md` and `senra-status.sh` existed **only as untracked files on one laptop** — never committed to any ref, any path; `docs/phase-1-spec.md` was the only doc any branch carried, and `SENRA-MOBILE-REDESIGN.md` was tracked at the **repo root** three days behind the working copy. No reviewer could fetch what they were asked to review against. **The board's own §9 check (`senra-status.sh:206`) was written to catch exactly this and had never been able to run**, because the script it checks for was itself untracked and sitting at `docs/senra-status.sh` while `PROTOCOL.md:19`/`§4` and `HANDOVER.md:20,30` all point at `scripts/senra-status.sh` — so every session that followed "First moves" step 2 hit *no such file or directory*. Fixed in **`e81e5be`** (`docs/mobile-redesign`, pushed): all four docs tracked at `docs/`, the board at `scripts/` +x, `SENRA-MOBILE-REDESIGN.md` root→`docs/` with content advanced to the 2026-07-17 state, and `.gitignore` extended to cover the §5 root landmines. **`docs/mobile-redesign` → `main` is verified clean and still owed** (`merge-tree` exit 0; net three-dot diff is docs-only — `253e9ff`/`a1f0f60` cancel; `.gitignore` identical at base and on `main`). **Q6's root-vs-`docs/` contradiction recorded as G-home (§6) — not silently fixed; the ruling is Viktor's.** — **Nothing was backed up.** `origin` had never seen **either** phase1 branch: C0-iOS/C1-iOS (`b3f0fcf`, `679a8a1`, `9c50b25`) and C0-Android/C2-Android (`51b8965`, `6263693`) — every reviewed, device-verified chunk of this phase — existed on one disk. **All pushed and verified against the remote**; Android's 3 genuinely-unpushed side branches (`043b361` snackbar, `e16fc70` FAQ copy, `92fd3c7` i18n sweep) pushed too. — **Three "unpushed" commits turned out to be superseded drafts and were PARKED, not pushed** (annotated `park/*` tags, GC-safe, local): `d698262` (#112 Waze) is a 00:02 draft rewritten by Viktor at 08:54 as `d25f1ea`, which correctly dropped the `MapAppPickerDialog.kt` hunk because `5bfa703` (H46) had landed the day before — `origin/main` has 3 `resolveActivity`, the draft 5; `081cf22` (ws-i) carries the **identical author timestamp** to origin's `7e857f8`, which deliberately dropped `AppCheckInterceptor.kt` because `82dd50b` (H47) made it **fail-closed** the day before — `origin/main` reads fail-closed ×11, and the draft's comment argues fail-**open** is intentional, i.e. **landing it would plant a comment that contradicts its own file (Rule 3)**; `e134cd9` duplicates C7, already shipped as `e6181e6`. Both drafts are non-fast-forward: pushing needed `--force` and would have dropped 13 and 15 commits from origin. **CC declined the instruction to push them and surfaced instead** — the instruction rested on CC's own earlier report that they were at-risk work, which was a premature finding (§9.12's pattern, self-inflicted this time). — **Android: `f0c5aa3`** (`chore/android-senra-pointer-and-gitignore`, off `main`, **unpushed**) — `SENRA.md`, a pointer that states no facts (§2 locked decision), + root-anchored landmine ignores; Android's untracked-stray count went **6 → 0**. Patterns are root-anchored because a bare `*.png` would have shadowed all **167** tracked PNGs under `app/src/**`, and `/CLAUDE-*.md` carries the hyphen so the tracked, legitimate `CLAUDE.md` survives; **11 already-tracked root strays match the new patterns and stay tracked** — recorded in the commit body, not hidden. **The board's §7 log-behind-code check stays green:** it greps this plan for each repo's phase1 short tip — `9c50b25` (§9.7) and `6263693` (§9.11) are both logged.
