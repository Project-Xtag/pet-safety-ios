// Shared build settings across all configurations.
// The `//` separator inside URLs must be escaped as `/$()/` in xcconfig
// because `//` starts a comment.

PRODUCT_BUNDLE_IDENTIFIER_BASE = com.petsafety.PetSafety
